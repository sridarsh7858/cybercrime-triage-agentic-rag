# Mirror of [project.dependencies] in pyproject.toml, for pip users. uv users
# should install from pyproject.toml / uv.lock instead — if you add a dependency,
# add it to both.

# --- Backend API Framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9
pydantic>=2.0.0

# --- Agentic RAG Core (LangGraph state machine + LangChain/Ollama) ---
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-chroma>=0.1.0
langchain-ollama>=0.1.0

# --- Vector Database Engine ---
chromadb>=0.5.0

# --- OCR & Computer Vision ---
easyocr>=1.7.0
pillow>=10.0.0
torch
torchvision

# --- Optional: recording the README preview video (scripts/record_preview.py) ---
# playwright>=1.45.0